extends "res://scripts/main_v083.gd"

# Compatibility shim only. The prior v084 experiment targeted the older
# scripted-stop v083 lineage and became invalid when v083 was concurrently
# replaced by the physical O-Ring implementation. Main.tscn does not use v084.
const VERSION_084 := "0.5.16-dev"
